# Docker Environment Variables
# Copy this file to .env and update with your actual values

# Database Configuration
POSTGRES_DB=order_entry_db
POSTGRES_USER=order_user
POSTGRES_PASSWORD=order_password

# Email Configuration (Optional - for email simulation)
EMAIL_USER=demo@example.com
EMAIL_PASSWORD=demo-password

# Database URL for Backend (Docker version)
DATABASE_URL=postgresql://order_user:order_password@database:5432/order_entry_db

# Application settings
ENVIRONMENT=production
DEBUG=False
